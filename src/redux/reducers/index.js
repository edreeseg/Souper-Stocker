import {
  LOADING,
  GET_INVENTORY,
  GET_KITCHENS,
  EDIT_INVENTORY,
  LOGIN,
} from '../reducers';

const initialState = {};

const reducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}

export default reducer;